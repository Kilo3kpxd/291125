
from typing import List

class Solution:
    def minOperations(self, nums: List[int], k: int) -> int:
        # Calculate the total sum of the array
        total_sum = sum(nums)
        
        # The remainder tells us how many times we need to subtract 1
        # Each operation reduces the sum by 1
        return total_sum % k
